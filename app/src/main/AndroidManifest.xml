<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <!-- Add internet permission for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".UniforGymApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Unifor_gym"
        android:usesCleartextTraffic="true"
        tools:targetApi="31" >

        <activity
            android:name=".activities.VLibrasActivity"
            android:exported="false"
            android:theme="@style/Theme.Unifor_gym" />
        <activity
            android:name=".activities.Chat"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />
        <activity
            android:name=".activities.AulaDetalhes"
            android:exported="false" />
        <activity
            android:name=".activities.ConfiguracoesPerfil"
            android:exported="false" />
        <activity
            android:name=".activities.ExercicioDetalhes"
            android:exported="false" />
        <activity
            android:name=".activities.UserActivity"
            android:exported="false" />
        <activity
            android:name=".activities.RecuperarSenha"
            android:exported="false" />
        <activity
            android:name=".activities.Cadastro"
            android:exported="false" />
        <activity
            android:name=".activities.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.AdminActivity"
            android:exported="false">
        </activity>

    </application>

</manifest>